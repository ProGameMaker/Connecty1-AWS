<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".InfomationActivity"
    >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <ImageView
            android:id="@+id/backgroundPic"
            android:src="@drawable/a4"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"

            />
    </FrameLayout>






    <LinearLayout
        android:orientation="vertical"
        android:layout_marginTop="220dp"
        android:background="@color/blackWhite"
        android:layout_width="match_parent"
        android:layout_height="match_parent"


        >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="30dp"
            android:layout_marginTop="40dp"
            android:layout_marginRight="30dp"
            android:elevation="5dp"
            android:text="About me"
            android:textSize="30dp"
            android:textStyle="bold"
            android:background="@drawable/shape"
            android:paddingLeft="20dp"
            android:paddingRight="20dp"
            />


        <TextView

            android:layout_marginLeft="30dp"
            android:layout_marginRight="30dp"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="30dp"
            android:padding="20dp"

            android:background="@drawable/shape"

            android:id="@+id/infoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:lineSpacingExtra="4dp"
            android:scrollbars="vertical"
            android:text="It's worth noting that you should also remove the values from these hidden inputs if the user removes a selected location and then manually enters a location. It's worth noting that you should also remove the values from these hidden inputs if the user removes a selected location and then manually enters a location. It's worth noting that you should also remove the values from these hidden inputs if the user removes a selected location and then manually enters a location. It's worth noting that you should also remove the values from these hidden inputs if the user removes a selected location and then manually enters a location. It's worth noting that you should also remove the values from these hidden inputs if the user removes a selected location and then manually enters a location.\n\n\n"
            android:textSize="20dp" />

    </LinearLayout>

    <LinearLayout
        android:layout_gravity="right|bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp">

        <Button
            android:backgroundTint="@color/colorPrimary"
            android:text="Call"
            android:onClick="Call"
            android:textColor="@color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <Button
            android:backgroundTint="@color/colorPrimary"
            android:text="Order"
            android:onClick="Next"
            android:textColor="@color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <Button
        android:backgroundTint="@color/colorPrimary"
        android:layout_margin="10dp"
        android:layout_gravity="bottom"
        android:text="Choose Another"
        android:onClick="Rerandom"
        android:textColor="@color/white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />


    <LinearLayout
        android:background="@color/white"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="130dp"
        android:layout_marginLeft="30dp"
        android:padding="2dp"
        >

        <ImageView
            android:id="@+id/iconView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/a5" />


    </LinearLayout>



</FrameLayout>